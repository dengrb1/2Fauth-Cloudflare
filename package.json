{
  "name": "worker-2fauth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply worker-2fauth-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply worker-2fauth-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
